# Test version - minimal redirect only
# If this works, the issue was with Options or FilesMatch directives

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all requests to public/ directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

